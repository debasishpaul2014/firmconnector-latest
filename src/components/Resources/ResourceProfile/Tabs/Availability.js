import React, { useState, useEffect } from "react";

const Availability = (props) => {
  const { profileDetails } = props;

  return (
    <div className="d-block">
      <span className="p-wrap">Availability</span>
    </div>
  );
};

export default Availability;
